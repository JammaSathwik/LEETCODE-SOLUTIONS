class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int x=0,i,s=0;
        for(i=0;i<gain.size();i++)
        {
            s=s+gain[i];
            x=max(x,s);
        }
        return x;
    }
};
