class Solution 
{
public:
    int missingNumber(vector<int>& nums) 
    {
         int x=nums.size()*(nums.size()+1)/2;
        for(int i=0;i<nums.size();i++)
        {
            x=x-nums[i];
        }
        return x;
    }
};
