class Solution {
public:
    int getDecimalValue(ListNode* head) {
        int x=0;
        while(head!=NULL)
        {
            x=x*2+head->val;
            head=head->next;
        }
        return x;
    }
};
