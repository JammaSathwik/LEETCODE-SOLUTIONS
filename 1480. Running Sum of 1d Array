class Solution 
{
public:
    vector<int> runningSum(vector<int>& nums) 
    {
        vector<int> x;
        int i,sum=0;
        for(i=0;i<nums.size();i++)
        {
            sum = sum + nums[i];
            x.push_back(sum);
        }
        return x;
    }
};
